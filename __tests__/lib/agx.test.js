/**
 * Basic sanity tests for the agx project.
 */

describe('agx', () => {
  test('truthy sanity check', () => {
    expect(true).toBe(true);
  });

  test('basic arithmetic', () => {
    expect(1 + 1).toBe(2);
  });
});
